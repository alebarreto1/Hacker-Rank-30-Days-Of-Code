
    // Posting here *only* the Submited code on hacker hank's editor screen. In order to make it run on your vs code (or similar) you need to make the necessary implementations
    
    
    // Declare second integer, double, and String variables.
    let secondInteger, secondDouble, secondString
    // Read and save an integer, double, and String to your variables.
    secondInteger = parseInt(readLine())
    secondDouble = parseFloat(readLine())
    secondString = readLine()
    // Print the sum of both integer variables on a new line.
    console.log(i + secondInteger)
    // Print the sum of the double variables on a new line.
    console.log(parseFloat(d + secondDouble).toFixed(1))
    // Concatenate and print the String variables on a new line
    // The 's' variable above should be printed first.
    console.log(s.concat(secondString))



//-----------------------------------------------------------------------------
    
    //One of the ways to successfully run the code on your vs code:

    // function main() {
    //     let i = 4
    //     let d = 4.0
    //     let s = "HackerRank "
    
    //     let secondInteger = 12
    //     let secondDouble = 4.0
    //     let secondString = "is the best place to learn and practice coding!"

    //     console.log(i + secondInteger)
    
    //     console.log(parseFloat(d + secondDouble).toFixed(1))

    //     console.log(s.concat(secondString))
    // }

    // main()
    